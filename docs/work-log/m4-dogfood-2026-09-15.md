# M4 Dogfood Session — Scheduled 2026-09-15

> **Type:** dated owner-override note (DR-006). Entry gate for Horizon B
> (`docs/plans/horizon-b-generate-evidence-via-dogfood-2026-09-12.md` §1) and the
> ADR-0031 extension condition.
> **Recorded:** 2026-09-14. **Session date:** 2026-09-15.

## Decision

Owner booked a co-maintainer dogfood session for **2026-09-15** under the
DR-006 `owner-override` gate. This satisfies the ADR-0031 extension condition
("extend only with a dated dogfood session booked") and sets the new
ADR-0031 decision-window close to **2026-09-15**.

## Scope (DR-006 T4 M4 carve-out)

**Background lifecycle + operator cockpit only.** Cloud, gateway, and
multi-tenant surfaces remain held per DR-006 T4 — this session does not
authorize them.

## What the owner runs

Per Horizon B B1 — real coding through the harness, owner-observed only
(agents must not simulate testimony):

- `teaagent tui --setup --root .`
- `teaagent agent run` with subagents
- background `attach` lifecycle
- cockpit inspection

## Evidence collected after the session (agent-run, B2/B3)

- Re-run `scripts/prepare_h4_evidence.py --since 2026-08-13 --until 2026-09-15`
  and `scripts/build_h4_decision_packet.py` against the real audit logs (not
  the `/tmp/h4_demo.jsonl` spike) — organic `h4_governance_shadow` events are
  the real evidence.
- BG-001 background lifecycle per
  `specs/background-lifecycle-acceptance-spec-2026-07-11.md` §8; cockpit per
  `specs/operator-cockpit-acceptance-spec-2026-07-11.md`.
- New friction entries, or an explicit dated "no friction" owner testimony.

## Exit evidence (B1)

A dated work-log `docs/work-log/m4-dogfood-2026-09-15.md` recording session
date, commands run, failures seen, organic `h4_governance_shadow` count, and
new friction entries or explicit "no friction" owner testimony.

## Partial agent PTY evidence (does NOT satisfy B1)

B1 requires real coding through the harness with owner-observed testimony
(agents must not simulate testimony). What follows is agent-driven PTY
evidence only — a placeholder until the owner session runs.

Date: 2026-09-15. A co-maintainer agent exercised the TUI surface under a
real pseudo-TTY (`pty.openpty`). Session transcript:
`.teaagent/tui-dogfood-2026-09-15-pty.txt`. Commands exercised: `help`,
`permission`, `daily`, `session list`, `approvals pending`, `ask`, `exit`.
Run `9d2f7db87eaf4089b1f739088027126a` was produced in `prompt` mode with the
`fake` provider — **0 tool calls, 0 files changed, 0 new shadow receipts**.
The TUI start/repl/exit lifecycle is functional under a PTY, but this session
produced no organic coding evidence and cannot substitute for B1.

Two failures observed during session setup, both recorded not smoothed over:

- `teaagent tui` crashed on launch with `TypeError: expected str or
  os.PathLike` because `apply_config_defaults` passed the `root` sentinel
  (`_UNSET`) to `Path(root)`. Fixed in `8babc313`.
- `show last` in the TUI REPL raised `run 'last' not found` and **exited the
  REPL with code 1** instead of returning to the prompt — an unhandled
  exception in the command loop. Unfixed at session time; recorded as a residual failure.
  Non-PTY transcripts (`.teaagent/tui-dogfood-2026-09-15-transcript.txt`,
  heredoc stdin, "Input is not a terminal" warning) are labeled non-PTY.
  Postscript 2026-09-15: fixed in `5a858ae9`/`9c61981b` (single
  `handle_command` guard + `_cmd_show` FileNotFoundError/OSError catch);
  regression `tests/tui/test_tui_show_unknown_run.py`.

## B2/B3 evidence

- `prepare_h4_evidence.py --since 2026-08-13 --until 2026-09-15`: 8 in-log approval receipts + 1 orphan-sourced subagent_launch D1 candidate (9 observed `h4_governance_shadow` events across 21 reachable runs), 21
  reachable runs, 7,779 total events, verdict `needs_review`. Unchanged by
  the PTY session (no new receipts).
- `build_h4_decision_packet.py`: 4/5 criteria `prepared`; criterion 4
  (human sign-off) is `human_required` — the PTY transcript is recorded
  evidence, but the owner sign-off itself cannot be agent-generated (D1 also
  deferred).
- BG-001 background lifecycle and cockpit surfaces were exercised headless and
  in the TUI; see `docs/work-log/dogfood-findings-2026-09-15.md`.

## Friction / owner testimony

Residual failures above (TUI launch crash — fixed; `show last` REPL exit —
fixed post-session, see postscript above). No owner friction testimony exists: B1 is still pending the owner's
own session, and criterion 4 awaits owner sign-off on the ADR-0031 packet.
